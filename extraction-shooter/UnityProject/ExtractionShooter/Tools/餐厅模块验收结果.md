# 本次验收结果

日期：2026-09-06。状态：本次显示重构的自动回归与运行画面检查通过；完整经营/存档链路未验收。下表记录餐厅阶段结果；后续局内适配的最新回归为 13 项编辑态、9 项运行态、5 项正式场景全部通过，详见 `局内战斗透视验收.md`。提交及推送状态以 Git 记录为准。

## 改动

- 餐厅改为透视投影，2D 地面与立面分开处理。当前倾角从法线起 32°、FOV 40°；拖拽半径 1.5。
- 拆出独立 World/HUD 预制体、配对配置、镜头配置；通用锚点、UI ID 绑定、显示开关、旧弹层承载可复用。餐厅业务留在接入层。
- 餐厅改为两排三列展示桌椅、两个灶台和两个摆菜台；HUD 按参考的金币栏、队列、消息板、背包和按钮布局制作。
- 修复进入/退出镜头竞争、玩家返回时镜头偏移、透视近裁剪面、旧 UI/调试内容叠加、负距离旧菜单不可见问题。
- 队列保留原对象和引用；新增跨 Canvas 食材飞行显示适配，不改扣除、排队、经营容量、解锁和存档规则。
- 新增可逐步/自动执行的验收窗口、MCP 脚本、预制体与锚点检查、无资源变动的显示探针。

## 验证

| 检查 | 结果 |
|---|---|
| 框架 EditMode | 最新一轮 9/9 通过，包含 XY/XZ 平面及透视近大远小 |
| 框架 PlayMode | 最新一轮 9/9 通过，包含透视切换与负值正交近裁剪面恢复 |
| 正式场景回归 | 最新一轮 4/4 通过：小镇跟随、地牢死区/上限/UI 拦截、真实小镇→地牢→小镇流程、餐厅进出 |
| 餐厅逐步自动流程 | 最新一轮 7 步通过，共 18 条断言；实际 UI 射线命中菜谱、装修、出口；含重入和禁用接入组件恢复 |
| 菜谱实际点击 | 菜品选中、再次点击触发食材不足保护、数量不变、点击外部关闭均通过 |
| 食材飞行显示 | 合成显示探针通过：接入已绑定、图标可见、结束清理。不扣资源、不修改队列数据 |
| 独立预制 / 地编 | 临时预览场景实例化 World/HUD、整体移动、桌椅组移动及锚点跟随检查通过 |
| 分辨率 / 截图 | 1920×1080、1440×1080、2560×1080 实跑截图已检查；主要 HUD 不裁切。美术最终一致性仍需签收 |
| Console | 最后一轮实跑后读取 Error 为 0 条 |
| 完整经营链路、存档 | 未验收；本次自动脚本不增加资源、不改存档，不以相机测试代替经营验收 |

## 当前效果与边界

- 地面已呈近宽远窄，家具保留手绘立面；没有把整张餐厅效果图直接倾斜。
- 餐厅阶段只接入餐厅；后续已将 Layer1/Layer2/Layer3 接入战斗透视配置，小镇仍保留原正交跟随。不是全项目所有场景已改透视。
- 旧背景暂按区域拆出地面、后墙、前栏；侧栏/楼梯等精确还原需要独立源图。地图是可继续地编的首版，不承诺与地图效果图逐像素一致。
- 六张外观桌不代表新增六张经营桌；当前仍沿用原三张逻辑桌的 12 个座位及解锁归属。额外展示桌椅待业务需求。
- 菜谱、装修、出口接原逻辑；管理暂禁用。消息板、第二信息栏、四格背包外框目前是展示，未擅自扩展业务。
- 主角仍使用临时像素素材；旧菜谱和装修面板是兼容弹层，并未重新设计内部 UI。
- 使用隔离测试存档补验：食材扣除→排队→烹饪→出菜→顾客就坐→收入→退出重进保存。当前不能据此宣布完整经营功能验收完成。

原始记录：`Library/CameraAcceptanceResults/`、`Library/ModuleAcceptance/`。当前结果来自继续执行后的新一轮测试，不把先前中止的测试记作通过。
