using UnityEngine;

/// <summary>背包显隐等入口；格子动效见 <see cref="InventoryItemUI"/> / <see cref="InventoryManager"/>。</summary>
public class InventoryController : MonoBehaviour
{
    private void Awake()
    {
    }

    private void Start()
    {
    }
}